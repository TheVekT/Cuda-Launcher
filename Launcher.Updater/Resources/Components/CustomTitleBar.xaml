<UserControl
    Height="32"
    x:Class="Launcher.Updater.Resources.Components.CustomTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!--  Left: Logo & Title  -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Image
                Height="16"
                Margin="14,0,0,0"
                RenderOptions.BitmapScalingMode="HighQuality"
                Source="pack://application:,,,/icon.ico"
                VerticalAlignment="Center"
                Width="16" />
            <TextBlock
                FontFamily="{DynamicResource GlobalFont}"
                FontSize="12"
                FontWeight="Normal"
                Foreground="{DynamicResource ForegroundSecondaryBrush}"
                Margin="10,0,0,0"
                Text="{Binding Title, FallbackValue='Cuda Launcher — Updater'}"
                VerticalAlignment="Center" />
        </StackPanel>

        <!--  Right: Action Buttons (Minimize and Close only)  -->
        <StackPanel
            Grid.Column="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            shell:WindowChrome.IsHitTestVisibleInChrome="True">

            <Button
                Click="MinimizeButton_Click"
                Content="&#xE921;"
                Style="{StaticResource TitleBarButtonStyle}"
                x:Name="MinimizeButton" />

            <Button
                Click="CloseButton_Click"
                Content="&#xE8BB;"
                Style="{StaticResource TitleBarCloseButtonStyle}"
                x:Name="CloseButton" />
        </StackPanel>
    </Grid>
</UserControl>
