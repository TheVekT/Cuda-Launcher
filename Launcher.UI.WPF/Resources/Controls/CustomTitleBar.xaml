<UserControl
    Height="32"
    x:Class="Launcher.UI.WPF.Resources.Controls.CustomTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource BackgroundTransparentBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock
            Foreground="{DynamicResource ForegroundSecondaryBrush}"
            Margin="12,0,0,0"
            Text="{Binding Path=Title, RelativeSource={RelativeSource AncestorType=Window}}"
            VerticalAlignment="Center" />

        <StackPanel
            Grid.Column="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            shell:WindowChrome.IsHitTestVisibleInChrome="True">

            <Button
                Click="MinimizeButton_Click"
                Content="&#xE921;"
                Style="{StaticResource TitleBarButtonStyle}"
                x:Name="MinimizeButton" />

            <Button
                Click="MaximizeButton_Click"
                Content="&#xE922;"
                Style="{StaticResource TitleBarButtonStyle}"
                x:Name="MaximizeButton" />

            <Button
                Click="CloseButton_Click"
                Content="&#xE8BB;"
                Style="{StaticResource TitleBarCloseButtonStyle}"
                x:Name="CloseButton" />

        </StackPanel>
    </Grid>
</UserControl>