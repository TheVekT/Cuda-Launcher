<Window
    Background="{DynamicResource AppBackgroundBrush}"
    FontFamily="{DynamicResource GlobalFont}"
    FontWeight="Medium"
    Height="720"
    Margin="0"
    MinHeight="720"
    MinWidth="1280"
    TextOptions.TextFormattingMode="Display"
    Title="{helpers:Loc MainWindow_Title}"
    UseLayoutRounding="True"
    Width="1280"
    WindowStyle="SingleBorderWindow"
    d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
    mc:Ignorable="d"
    x:Class="Launcher.UI.WPF.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:accounts="clr-namespace:Launcher.UI.WPF.ViewModels.Accounts"
    xmlns:behaviors="clr-namespace:Launcher.UI.WPF.Behaviors"
    xmlns:common="clr-namespace:Launcher.UI.WPF.Views.Common"
    xmlns:components="clr-namespace:Launcher.UI.WPF.Views.Common.Components"
    xmlns:config="clr-namespace:Launcher.UI.WPF.ViewModels.Config"
    xmlns:config1="clr-namespace:Launcher.UI.WPF.Views.Config"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enums="clr-namespace:Launcher.Core.Common.Enums;assembly=Launcher.Core"
    xmlns:game="clr-namespace:Launcher.UI.WPF.ViewModels.Game"
    xmlns:game1="clr-namespace:Launcher.UI.WPF.Views.Game"
    xmlns:helpers="clr-namespace:Launcher.UI.WPF.Helpers"
    xmlns:identity="clr-namespace:Launcher.UI.WPF.Views.Identity"
    xmlns:instances="clr-namespace:Launcher.UI.WPF.ViewModels.Instances"
    xmlns:instances1="clr-namespace:Launcher.UI.WPF.Views.Instances"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:notifications1="clr-namespace:Launcher.UI.WPF.Views.Common.Notifications"
    xmlns:settings="clr-namespace:Launcher.UI.WPF.ViewModels.Settings"
    xmlns:settings1="clr-namespace:Launcher.UI.WPF.ViewModels.Common"
    xmlns:settings2="clr-namespace:Launcher.UI.WPF.Views.Settings"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
    xmlns:viewModels="clr-namespace:Launcher.UI.WPF.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Window.Resources>
        <helpers:StringToResourceConverter x:Key="StringToResourceConverter" />
        <helpers:TypeMatchConverter x:Key="TypeMatchConverter" />


        <DataTemplate DataType="{x:Type instances:AddInstanceViewModel}">
            <instances1:AddInstanceView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type instances:EditInstanceViewModel}">
            <instances1:EditInstanceView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type settings1:ConfirmViewModel}">
            <common:ConfirmView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type config:CustomizationViewModel}">
            <config1:CustomizationView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type settings1:ProgressViewModel}">
            <common:ProgressView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type accounts:LoginViewModel}">
            <identity:LoginView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type settings:SettingsViewModel}">
            <settings2:SettingsView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type game:SkinEditorViewModel}">
            <game1:SkinEditorView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type game:CrashViewModel}">
            <game1:CrashView />
        </DataTemplate>

    </Window.Resources>
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="1"
            CornerRadius="{DynamicResource DefaultCorner}"
            GlassFrameThickness="0"
            ResizeBorderThickness="6"
            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <components:CustomTitleBar Grid.Row="0" />
        <Grid
            AllowDrop="{Binding AppStore.IsOverlayVisible, Converter={StaticResource NotEqualConverter}, ConverterParameter=True}"
            Grid.Row="1"
            PreviewDragEnter="UIElement_OnDragEnter"
            PreviewDragLeave="UIElement_OnDragLeave"
            PreviewDrop="UIElement_OnDrop">
            <Grid.LayoutTransform>
                <ScaleTransform ScaleX="{Binding SettingsViewModel.SettingsStore.UiScale}" ScaleY="{Binding SettingsViewModel.SettingsStore.UiScale}" />
            </Grid.LayoutTransform>
            <Grid x:Name="MainPanel">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid
                    Background="{DynamicResource BackgroundTransparentBrush}"
                    Grid.Row="0"
                    Height="45"
                    VerticalAlignment="Top"
                    x:Name="Header">
                    <Border
                        BorderBrush="{DynamicResource BorderPrimaryBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="20,6">
                        <Grid x:Name="HeaderContainer">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel
                                    Grid.Column="1"
                                    HorizontalAlignment="Left"
                                    Orientation="Horizontal"
                                    x:Name="NavContainer">
                                    <RadioButton
                                        Command="{Binding NavigateCommand}"
                                        CommandParameter="Play"
                                        Height="30"
                                        IsChecked="True"
                                        Padding="10,0"
                                        Style="{StaticResource NavRadioButtonStyle}"
                                        x:Name="PlayRadio">
                                        <RadioButton.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <Viewbox
                                                    Height="16"
                                                    Stretch="Uniform"
                                                    Width="16">
                                                    <Grid Height="16" Width="16">
                                                        <Path
                                                            Data="{StaticResource Icon.Block}"
                                                            Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"
                                                            StrokeThickness="1.33" />
                                                    </Grid>
                                                </Viewbox>
                                                <TextBlock
                                                    FontSize="14"
                                                    Margin="6,0,0,0"
                                                    Text="{helpers:Loc MainWindow_Navigation_Play}" />
                                            </StackPanel>
                                        </RadioButton.Content>
                                    </RadioButton>

                                    <RadioButton
                                        Command="{Binding NavigateCommand}"
                                        CommandParameter="Installations"
                                        Height="30"
                                        Margin="6,0,0,0"
                                        Padding="10,0"
                                        Style="{StaticResource NavRadioButtonStyle}"
                                        x:Name="InstallationsRadio">
                                        <RadioButton.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <Viewbox
                                                    Height="16"
                                                    Stretch="Uniform"
                                                    Width="16">
                                                    <Grid Height="16" Width="16">
                                                        <Path
                                                            Data="{StaticResource Icon.Settings}"
                                                            Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"
                                                            StrokeThickness="1.33" />
                                                    </Grid>
                                                </Viewbox>
                                                <TextBlock
                                                    FontSize="14"
                                                    Margin="6,0,0,0"
                                                    Text="{helpers:Loc MainWindow_Navigation_Installations}" />
                                            </StackPanel>
                                        </RadioButton.Content>
                                    </RadioButton>

                                    <RadioButton
                                        Command="{Binding NavigateCommand}"
                                        CommandParameter="Skins"
                                        Height="30"
                                        Margin="6,0,0,0"
                                        Padding="10,0"
                                        Style="{StaticResource NavRadioButtonStyle}"
                                        x:Name="SkinsRadio">
                                        <RadioButton.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <Viewbox
                                                    Height="16"
                                                    Stretch="Uniform"
                                                    Width="16">
                                                    <Grid Height="16" Width="16">
                                                        <Path
                                                            Data="{StaticResource Icon.Skin}"
                                                            Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}"
                                                            StrokeThickness="1.33" />
                                                    </Grid>
                                                </Viewbox>
                                                <TextBlock
                                                    FontSize="14"
                                                    Margin="6,0,0,0"
                                                    Text="{helpers:Loc MainWindow_Navigation_Skins}" />
                                            </StackPanel>
                                        </RadioButton.Content>
                                    </RadioButton>
                                </StackPanel>
                                <Button
                                    Command="{Binding OpenLoginCommand}"
                                    FontSize="14"
                                    Grid.Column="2"
                                    Height="30"
                                    HorizontalAlignment="Right"
                                    Margin="6,0,0,0"
                                    Tag="{StaticResource Icon.Account}"
                                    helpers:ButtonHelper.IconSize="14"
                                    x:Name="LoginButton">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource AccentButtonStyle}" TargetType="Button">

                                            <Setter Property="Content" Value="{helpers:Loc MainWindow_Header_LoginButton}" />
                                            <Style.Triggers>

                                                <DataTrigger Binding="{Binding IdentityStore.IsLoggedIn}" Value="True">
                                                    <Setter Property="Content" Value="{Binding IdentityStore.CurrentAccount.Username}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IdentityStore.IsLoggedIn}" Value="False">
                                                    <Setter Property="Content" Value="{helpers:Loc MainWindow_Header_LoginButton}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
                <Grid Grid.Row="1" x:Name="Main">
                    <Border x:Name="FooterAndHeaderBackground">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding AppStore.CurrentBannerPath}" Stretch="UniformToFill" />
                        </Border.Background>
                        <Border.Effect>
                            <BlurEffect Radius="0" />
                        </Border.Effect>
                        <Border>
                            <Border.Background>
                                <LinearGradientBrush EndPoint="0.5, 1" StartPoint="0.5, 0">
                                    <GradientStop Color="#3D000000" Offset="0.0" />

                                    <GradientStop Color="#93000000" Offset="1.0" />
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Border>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid
                            Background="{DynamicResource BackgroundTransparentBrush}"
                            Grid.Row="1"
                            Height="47"
                            x:Name="Footer">
                            <Border
                                BorderBrush="{DynamicResource BorderPrimaryBrush}"
                                BorderThickness="0,1,0,0"
                                Padding="20,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <ComboBox
                                            Height="35"
                                            IsEnabled="{Binding IsEnabledInstancesComboBox}"
                                            ItemContainerStyle="{StaticResource InstanceItemContainerStyle}"
                                            ItemTemplate="{StaticResource InstanceDataTemplate}"
                                            ItemsSource="{Binding InstancesStore.Instances}"
                                            SelectedItem="{Binding InstancesStore.SelectedInstance}"
                                            Style="{StaticResource InstanceComboBox}"
                                            Width="300"
                                            helpers:ComboBoxHelper.PopupPlacement="Top" />
                                        <Button
                                            Command="{Binding LaunchCommand}"
                                            FontSize="16"
                                            Height="35"
                                            HorizontalAlignment="Center"
                                            Margin="6,0,0,0"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Visibility="{Binding AppStore.ShowCompactPlayButton, Converter={StaticResource BoolToVis}}"
                                            Width="35"
                                            helpers:ButtonHelper.IsLoading="{Binding AppStore.IsDownloading}"
                                            x:Name="PlayButtonCompact" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" />
                                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                                        <Button
                                            BorderThickness="{DynamicResource DefaultBorderThickness}"
                                            Command="{Binding InstallationsVM.OpenInstanceFolderCommand}"
                                            Height="35"
                                            Padding="0"
                                            Style="{StaticResource DefaultButtonStyle}"
                                            Tag="{StaticResource Icon.Folder}"
                                            Width="35"
                                            x:Name="OpenFolderButton">
                                            <Button.ContextMenu>
                                                <ContextMenu Style="{StaticResource ContextMenuStyle}">
                                                    <MenuItem Command="{Binding InstallationsVM.OpenInstanceFolderCommand}" Header="{helpers:Loc MainWindow_Footer_OpenFolder_ContextMenu_OpenInstanceFolder}" />

                                                    <MenuItem Command="{Binding InstallationsVM.OpenModsFolderCommand}" Header="{helpers:Loc MainWindow_Footer_OpenFolder_ContextMenu_OpenInstanceModsFolder}" />

                                                    <Separator />
                                                    <MenuItem Command="{Binding InstallationsVM.OpenRootFolderCommand}" Header="{helpers:Loc MainWindow_Footer_OpenFolder_ContextMenu_OpenRootMinecraftFolder}" />
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                        </Button>
                                        <Button
                                            BorderThickness="{DynamicResource DefaultBorderThickness}"
                                            Command="{Binding OpenSettingsCommand}"
                                            Height="35"
                                            Margin="6,0,0,0"
                                            Padding="0"
                                            Style="{StaticResource DefaultButtonStyle}"
                                            Tag="{StaticResource Icon.Settings}"
                                            Width="35"
                                            x:Name="SettingsButton" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                        <Grid Grid.Row="0" x:Name="TabsContainer">
                            <instances1:InstallationsView DataContext="{Binding InstallationsVM}">
                                <instances1:InstallationsView.Style>
                                    <Style TargetType="UserControl">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AppStore.CurrentView, Converter={StaticResource TypeMatchConverter}, ConverterParameter=InstallationsViewModel}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </instances1:InstallationsView.Style>
                            </instances1:InstallationsView>

                            <game1:PlayView DataContext="{Binding PlayVM}">
                                <game1:PlayView.Style>
                                    <Style TargetType="UserControl">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AppStore.CurrentView, Converter={StaticResource TypeMatchConverter}, ConverterParameter=PlayViewModel}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </game1:PlayView.Style>
                            </game1:PlayView>

                            <game1:SkinsView DataContext="{Binding SkinsVM}">
                                <game1:SkinsView.Style>
                                    <Style TargetType="UserControl">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AppStore.CurrentView, Converter={StaticResource TypeMatchConverter}, ConverterParameter=SkinsViewModel}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </game1:SkinsView.Style>
                            </game1:SkinsView>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
            <Grid
                Panel.ZIndex="999"
                Visibility="{Binding AppStore.IsOverlayVisible, Converter={StaticResource BoolToVis}}"
                x:Name="OverlayContainer">

                <Border Background="#10000000">
                    <Border.InputBindings>
                        <MouseBinding Command="{Binding CloseOverlayCommand}" MouseAction="LeftClick" />
                    </Border.InputBindings>
                </Border>

                <Grid
                    HorizontalAlignment="Center"
                    Margin="0,0,0,32"
                    RenderTransformOrigin="0.5,0.5"
                    VerticalAlignment="Center"
                    x:Name="OverlayWrapper">
                    <Grid.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1" x:Name="OverlayScale" />
                    </Grid.RenderTransform>

                    <Border Background="{DynamicResource OverlayBackgroundSurfaceBrush}" CornerRadius="{DynamicResource DefaultCorner}">
                        <Border.Effect>
                            <DropShadowEffect
                                BlurRadius="120"
                                Color="Black"
                                Opacity="1"
                                RenderingBias="Quality"
                                ShadowDepth="0" />
                        </Border.Effect>
                    </Border>

                    <ContentControl Content="{Binding AppStore.CurrentOverlayView}" IsHitTestVisible="True" />

                    <Border
                        Background="White"
                        CornerRadius="{DynamicResource DefaultCorner}"
                        IsHitTestVisible="False"
                        Opacity="0"
                        x:Name="FlashOverlay" />
                </Grid>

            </Grid>

            <!--  Notifications overlay slot  -->
            <ItemsControl
                HorizontalAlignment="Right"
                ItemsSource="{Binding NotificationService.Notifications}"
                Margin="0,55,20,55"
                Panel.ZIndex="9999"
                VerticalAlignment="Top">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}">
                            <ContentControl.Style>
                                <Style TargetType="ContentControl">
                                    <Style.Triggers>

                                        <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NotificationType.Info}">
                                            <Setter Property="ContentTemplate">
                                                <Setter.Value>
                                                    <DataTemplate>
                                                        <notifications1:InfoNotify />
                                                    </DataTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NotificationType.Success}">
                                            <Setter Property="ContentTemplate">
                                                <Setter.Value>
                                                    <DataTemplate>
                                                        <notifications1:SuccessNotify />
                                                    </DataTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NotificationType.Warning}">
                                            <Setter Property="ContentTemplate">
                                                <Setter.Value>
                                                    <DataTemplate>
                                                        <notifications1:WarnNotify />
                                                    </DataTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding Type}" Value="{x:Static enums:NotificationType.Error}">
                                            <Setter Property="ContentTemplate">
                                                <Setter.Value>
                                                    <DataTemplate>
                                                        <notifications1:ErrorNotify />
                                                    </DataTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>

                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!--  Drag & Drop Overlay  -->
            <Grid
                AllowDrop="True"
                Background="Transparent"
                Panel.ZIndex="1000"
                Visibility="{Binding AppStore.IsDragDropActive, Converter={StaticResource BoolToVis}}"
                behaviors:FileDragDropBehavior.DragEnterCommand="{Binding DragEnterCommand}"
                behaviors:FileDragDropBehavior.DragLeaveCommand="{Binding DragLeaveCommand}"
                behaviors:FileDragDropBehavior.DropCommand="{Binding DropCommand}"
                x:Name="DragDropOverlay">

                <Border Background="{DynamicResource BackgroundTransparentBrush}" Padding="10">
                    <Grid Margin="10">

                        <Border Background="{DynamicResource BackgroundSurfaceBrush}" CornerRadius="{DynamicResource SecondaryCorner}">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="120"
                                    Color="Black"
                                    Opacity="1"
                                    RenderingBias="Quality"
                                    ShadowDepth="0" />
                            </Border.Effect>

                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Viewbox
                                    Height="40"
                                    Margin="0,0,0,10"
                                    Stretch="Uniform"
                                    Width="40">
                                    <Path
                                        Data="{StaticResource Icon.Upload}"
                                        Stroke="{DynamicResource ForegroundSecondaryBrush}"
                                        StrokeThickness="1.5" />
                                </Viewbox>
                                <TextBlock
                                    FontSize="24"
                                    FontWeight="Bold"
                                    Foreground="{DynamicResource ForegroundPrimaryBrush}"
                                    HorizontalAlignment="Center"
                                    Margin="0,10,0,8"
                                    Text="{helpers:Loc MainWindow_DragDropOverlay_Title}" />
                                <TextBlock
                                    FontSize="16"
                                    Foreground="{DynamicResource ForegroundSecondaryBrush}"
                                    HorizontalAlignment="Center"
                                    Text="{helpers:Loc MainWindow_DragDropOverlay_Subtitle}" />
                            </StackPanel>
                        </Border>

                        <Rectangle
                            IsHitTestVisible="False"
                            RadiusX="6"
                            RadiusY="6"
                            Stroke="{DynamicResource BorderSecondaryBrush}"
                            StrokeDashArray="4 2"
                            StrokeThickness="1" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
